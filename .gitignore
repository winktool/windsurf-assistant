# 道法自然 — 专注切号助手本源, 隔离一切非核心资源于本地

# ═══ 核心资源隔离 (仅保留本地, 不入库) ═══
cloud-pool/
diagnostics/
engine/
media/
monitor/
pipeline/
pool-admin/
research/
scripts/
tools/
dist/

# ═══ 账号池数据 — 绝不入库 ═══
*.auth_blob
accounts.json
keypool.json
oneshot_token.json
inject_result.json
quota_snapshots.json
inuse_marks.json
instance_claims.json

# ═══ 机密 ═══
.env
.env.local
secrets.env
*.pem
*.key

# ═══ 构建产物 ═══
*.vsix
node_modules/

# ═══ 临时与日志 ═══
*.log
*.bak
__pycache__/

# ═══ 本地映射 ═══
LOCAL_MAPPING.md
.vsixmanifest

# ═══ 允许 wam-bundle 内图标 ═══
!wam-bundle/media/
